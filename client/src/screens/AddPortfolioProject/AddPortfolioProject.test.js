/**
 * Renders successfully (snapshot test)
 * OnClick "save" will:
 *    - add the current project to the user's "portfolio projects" array
 *    - open modal
 *    - in modal: user clicks "add another project" => close modal and clear form
 *      - project renders below the form to display all added projects
 *    - in modal: user clicks "finish sign up" => close modal and navigate user to user profile
 * OnClick "finish" will navigate user to user profile
 */
