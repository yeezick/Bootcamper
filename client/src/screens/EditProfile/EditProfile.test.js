/**
 * import component
 * 
 * mock dispatch 
 * 
 * provide the UI data that would get processed
 * 
 * check that dispatch is called with the expected values 
 *  - no need to check that state is updated because i can trust dispatch to do its job
 *  - just want to check that dispatch is being called with the right inputs
 * 
 */