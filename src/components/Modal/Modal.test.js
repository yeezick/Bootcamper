/**
 * Generic Modal (white, text, sometimes buttons) => portfolio project saved flow => applicant accepted flow
 * Modal text should be handled dynamically
 *    test with different texts
 *    verify that the text also appears on the screen
 * Modal may have several buttons
 *    2-3 buttons, each with their own onpress function
 *    test that these buttons do work on click
 * Modal may not have any buttons
 *    text will render without any buttons
 *    modal will also disappear after 3 seconds
 */
